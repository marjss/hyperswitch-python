version: '3.9'
services:
  app:
    build: .
    volumes:
      - .:/work:delegated
    working_dir: /work
    ports:
      - "8000:8000"
    environment:
      PYTHONDONTWRITEBYTECODE: 1
      PYTHONUNBUFFERED: 1
    command: ["bash"]
